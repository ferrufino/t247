# Course access policy
class CoursePolicy < ApplicationPolicy
  def index?
    user.admin? || user.teacher?
  end

  def new?
    user.admin?
  end

  def create?
    user.admin?
  end

  def edit?
    user.admin?
  end

  def update?
    user.admin?
  end

  def destroy?
    user.admin?
  end
end
